# JATS Validate Helm Package

This package contains the helm package files for the JATS Validator, a web service used by production to test XML conforms to a specific schema making use of the schematron.